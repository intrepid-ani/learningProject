<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>E2C-<%= listing.title %></title>
</head>
<body>
    <h1 class="logo">E2C</h1>
    <hr>
    <h1><%= listing.title %></h1>
    <p><%= listing.description %></p>
    <p><%= listing.price.toLocaleString('en-IN', { style: 'currency', currency: 'INR' }) %></p>
    <hr>
    <a href="/">Back</a>
    <a href="/edit/<%= listing.id %>">Edit</a>
    <a href="/delete/<%= listing.id %>">Delete</a>
</body>
</html>